{
  "name": "pie",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.10.0",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "clean": "pnpm -r run clean",
    "build": "pnpm -r run build",
    "desktop:dev": "pnpm --filter pie dev",
    "desktop:build": "pnpm --filter pie build",
    "desktop:package": "pnpm --filter pie package",
    "desktop:dist": "pnpm --filter pie dist",
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r run test",
    "pack:dry-run": "pnpm -r --workspace-concurrency=1 exec pnpm pack --dry-run"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsx": "^4.23.0",
    "typescript": "^6.0.3"
  }
}
