{
  "name": "@pi-3.14/subagents",
  "version": "0.1.0",
  "description": "Concurrent subagent orchestration and tool integration for PI.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist", "README.md", "LICENSE"],
  "license": "MIT",
  "engines": {
    "node": ">=22.19.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "clean": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\"",
    "build": "pnpm --filter @pi-3.14/runtime build && tsc -p tsconfig.build.json",
    "pretest": "pnpm build",
    "test": "node --import tsx --test src/*.test.ts",
    "pretypecheck": "pnpm --filter @pi-3.14/runtime build",
    "typecheck": "tsc --noEmit",
    "prepack": "pnpm clean && pnpm build"
  },
  "dependencies": {
    "@pi-3.14/model": "workspace:^",
    "@pi-3.14/runtime": "workspace:^",
    "@earendil-works/pi-coding-agent": "^0.82.0",
    "typebox": "1.1.38"
  },
  "devDependencies": {
    "@types/node": "^26.1.1"
  }
}
