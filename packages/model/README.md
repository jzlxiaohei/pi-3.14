# `@pi-3.14/model`

Shared, JSON-safe TypeScript contracts for PI host state, events, turns, models,
usage, and fork results.

```ts
import type { PiHostEvent, PiTurnResult } from "@pi-3.14/model";
```

This package has no runtime dependencies.
